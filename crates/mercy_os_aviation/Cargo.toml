[package]
name = "mercy_os_aviation"
version = "1.0.0"
edition = "2021"
license = "MIT"
description = "MercyOS-Aviation — MercyOS-Pinnacle Extensions for Eternal Flight Resonance"

[dependencies]
nexi = { path = "../nexi" }
mercy_flight_agi = { path = "../mercy_flight_agi" }
tokio = { workspace = true }

# The example lives at the repository root because README.md documents it there.
[[example]]
name = "alpha_flight"
path = "../../examples/alpha_flight.rs"
