[package]
name = "mercy_electric_propulsion"
version = "0.1.0"
edition = "2021"
description = "MercyElectricPropulsion — Zero-Emission Distributed Electric Thrust Core"

[dependencies]
nexi = { path = "../nexi" }
