[package]
name = "mercy_electric_propulsion"
version = "1.0.0"
edition = "2021"
license = "MIT"
description = "MercyElectricPropulsion — Zero-Emission Electric Aircraft Thrust Core for Eternal Silent Flight"

[dependencies]
nexi = { path = "../" }
tokio = { version = "1.38", features = ["full"] }
